import { FormatCustomPipe } from './format-custom.pipe';

describe('FormatCustomPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCustomPipe();
    expect(pipe).toBeTruthy();
  });
});
